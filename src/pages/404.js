import React from 'react';
import Container from '@components/container';

class FlexibleTemplate extends React.Component {
  render() {
    return <Container slug={'/404'} type={'404'} />;
  }
}

export default FlexibleTemplate;
